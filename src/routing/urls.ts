/* eslint-disable @typescript-eslint/explicit-module-boundary-types */
const urls = {
  login: () => '/login',
  patients: () => '/patients',
  register: () => '/patients/register',
};

export default urls;
